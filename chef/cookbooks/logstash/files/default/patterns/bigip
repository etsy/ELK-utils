F5PROC (?:[\w.\(\)_/%-]+)
F5PROG %{F5PROC:proc_name}(?:\[%{POSINT:pid}\])?
F5DATE %{DAY} %{MONTH} %{MONTHDAY} %{TIME} %{YEAR}
F5USER (?:[\w.\(\)_/%-]+)
F5START start=\"%{F5DATE:start}\"
F5END end=\"%{F5DATE:end}\"

# This works with no IPV6 enabled, but will need the IPV6 address grokked properly to support both being present
BIGIPREFUSED  %{TIMESTAMP_ISO8601:timestamp} %{HOSTNAME:logsource} :: %{F5PROG:procname}: refused connect from ::ffff:%{IPV4:ip}

# The following is standard across login attempts
BIGIPPREFIX %{TIMESTAMP_ISO8601:timestamp} %{HOSTNAME:logsource} :: %{F5PROG:procname}: %{NUMBER}:%{NUMBER}: AUDIT - [Uu]ser %{F5USER:username} - RAW: %{F5PROG:raw_procname}: [Uu]ser=%{F5USER:raw_username}

# For BIGP refused connections
BIGIPREFUSED  %{TIMESTAMP_ISO8601:timestamp} %{HOSTNAME:logsource} :: %{F5PROG:procname}: refused connect from ::ffff:%{IPV4:ip}

# The format of a successful login seems to fit the following pattern
BIGIPLOGIN %{BIGIPPREFIX} partition=\[%{WORD:partition}\] level=%{WORD:level} tty=%{F5PROC:tty} host=%{HOSTNAME:client_host} attempts=%{POSINT:attempts} %{F5START}

# The format of a failed login seems to fit the following pattern
BIGIPLOGINFAILED %{BIGIPPREFIX} tty=%{F5PROC:tty} host=%{HOSTNAME:client_host} failed to login after %{POSINT:failed_attempts} attempts \(%{F5START:start}

# For everything else we don't care about, just accept * from the message
BIGIPIGNORED %{TIMESTAMP_ISO8601:timestamp} %{HOSTNAME:logsource} :: %{F5PROG:procname}:
